# Next Steps

## 現在地
- 要件定義は作成済み
- Colab 対応の改修設計を作成済み
- 次は Python 側の設定注入方式を実装可能な形まで固める

## 次にやること
1. `src` の改修対象を具体化する
2. `app_config.py` の項目を確定する
3. `excel_analyzer.py` の関数シグネチャ変更を実施する
4. 共通実行入口を追加する
5. Colab Notebook を追加する

## 実装開始条件
- 設計内容に大きな修正がないこと
- 出力フォルダルールが要件どおり `yyyyMM` で確定していること
- `分類.csv` の固定パス運用が要件どおりであること
